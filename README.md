# 🏦 Node.js Money Transfer App

A full-stack banking application that allows users to register, login, and securely transfer funds between accounts using JWT authentication.

## ✨ Features
* **User Authentication:** Secure registration and login using `bcrypt` and `JWT`.
* **Money Transfer:** Logic to handle balances and prevent over-drafting.
* **Dockerized:** Ready to run in any environment with a single command.
* **Frontend UI:** Simple JavaScript-driven interface.

## 🚀 How to Run Locally

### 1. Prerequisites
* [Node.js](https://nodejs.org/) (v18+)
* [Docker Desktop](https://www.docker.com/) (optional)

### 2. Setup
Clone the repository and install dependencies:
```bash
npm install
